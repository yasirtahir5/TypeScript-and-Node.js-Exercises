/* You should create four lines that look like this:

console.log(5 + 3)

Your output should simply be four lines with the number 8 appearing once on each line. */

console.log (`\t=================================\n`);
console.log (`\tAddition ${5+3}/n`);
console.log (`\tSubtraction ${10-2}/n`);
console.log (`\tMultiplication ${4*2}/n`);
console.log (`\tDivision ${16/2}/n`);
console.log (`\t=================================\n`);

//Program End